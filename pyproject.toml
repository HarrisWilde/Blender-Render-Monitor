[project]
name = "render-monitor-dev"
version = "0.0.0"
description = "Render Monitor add-on development environment"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = [
    "fake-bpy-module-latest>=20260902",
]
