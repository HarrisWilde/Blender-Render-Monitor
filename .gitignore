# Python
__pycache__/
*.pyc

# Reasonix 工具内部数据（会话、附件等）
.reasonix/

# Blender Python API 参考文档（外部素材，体积大，不纳入版本库）
reference/

# 构建产物（可从 render_monitor/ 源码重新打包）
render_monitor.zip

# 本地打包脚本（随个人工作流使用，不纳入版本库）
build.ps1
build.bat
